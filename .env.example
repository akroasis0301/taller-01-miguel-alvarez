# Copia este archivo como .env (en la raiz del repo) y reemplaza los marcadores.
# El .env real NUNCA se sube a git (esta en .gitignore).
OPENAI_API_KEY=AQUI PON TU API KEY
ANTHROPIC_API_KEY=AQUI PON TU API KEY
